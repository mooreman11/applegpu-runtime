[package]
name = "applegpu-python"
version.workspace = true
edition.workspace = true

[lib]
name = "applegpu_runtime"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module"] }
applegpu-core = { path = "../core" }
