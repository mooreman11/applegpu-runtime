[package]
name = "applegpu-core"
version.workspace = true
edition.workspace = true

[dependencies]
half = "2"
once_cell = "1"
const_format = "0.2"
applegpu-wire = { path = "../wire" }

[build-dependencies]

[lib]
name = "applegpu_core"
